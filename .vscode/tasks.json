{
    // Tarefa para compilar tetris.c
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar Tetris",
            "type": "shell",
            "command": "gcc",
            "args": [
                "tetris.c",
                "-o",
                "tetris.out"
            ],
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}
